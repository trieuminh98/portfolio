import { createStyles, Grid, makeStyles, ThemeProvider } from '@mui/material';
import './App.css';
import theme from './theme';

function App() {
  return (
    <ThemeProvider theme={theme}>
      <div>Helloxcxcxcxcxc</div>
    </ThemeProvider>
  );
}

export default App;
